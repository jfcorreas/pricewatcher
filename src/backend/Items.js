let items = {
  checkItem : function(item, store) {
    return ("TODO: check item info in store and update in database ");
  },
  getItem : function (item, store) {
    return ("TODO: get item info from database ");
  }
}

module.exports = items;
